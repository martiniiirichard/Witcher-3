# Quest / Content Backlog Triage

Date: 2026-06-27

## Scope

- `4670` - Night To Remember Quest Next-Gen
- `9157` - Boat Races
- `9206` - WaterwheelFIXED
- `9360` - W3QE / Witcher 3 Quests Extended
- `10744` - Fix for shoeless knight pants

## Current State

Keep the current split.

Active:

- `modBoatRaces`
- `dlcBoatRaces`
- `modwaterwheel2`
- `modknightbootfixes`
- `dlcW3QE`
- `dlcNightToRemember`

Disabled / reduced:

- `[modNightToRemember]` is disabled in `mods.settings`.
- `[modW3QE]` is disabled in `mods.settings`.
- `modNightToRemember` was moved out of active `Mods` after a no-window startup hang.
- `modW3QE` shell was archived after its cooked scene bundle caused the same no-window startup-hang pattern.

## Decisions

### Night To Remember

Do not reinstall the `modNightToRemember` half during backlog cleanup.

The prior failure was not a Script Merger issue. It was a pre-window loader/content hang. The DLC half remains present, but the mod half is disabled/quarantined. Revisit only as a dedicated content-package isolation pass.

### W3QE

Do not reactivate `modW3QE` blindly.

Prior isolation found that its `modW3QE` bundle contains the cooked scene:

- `quests\part_2\quest_files\q309_casablanca\scenes\q309_00u_ungrateful_mage_on_steak.w2scene`

That scene bundle caused a no-window startup hang. The DLC half remains installed; the mod shell is disabled/archived. This may preserve some DLC-side content, but the Moritz / An Ungrateful Mage scene integration is likely degraded.

### WaterwheelFIXED

Keep `modwaterwheel2`.

This is a narrow Toussaint q702 waterwheel layer fix with no scripts and no menu/XML payload. It already passed a DX12 launch smoke test.

### Boat Races

Keep `modBoatRaces` and `dlcBoatRaces`, but watch it during the next compile pass.

The active script folder still contains `br_inputManager_anno.ws`, which is tied to the earlier `IgnoreGameInput` duplicate native-export error. Because the stack later returned to a passing state, do not edit it blind here. If that compile error reappears, this is the first file to inspect.

### Shoeless Knight Pants

Keep `modknightbootfixes`.

This is a small cosmetic bugfix and does not appear to be a redundant gameplay/system owner.

## Status

- No game files changed in this triage pass.
- No Script Merger action run.
- No launch test run in this pass.
