# Equipment / Events / Stash Backlog Triage

Date: 2026-06-27

## Scope

- `5503` - All Gear Enchanting Based On Sockets
- `6033` - Additional Stash Locations + BiA Compatibility
- `6499` - Master Mirror Reward BiA patch
- `7488` - Events
- `7553` - Real Cheating Yen and Triss
- `7732` - Alcohol Factory
- `7955` - Quest Item Equipment

## Current State

Keep the active installs. They are not simple duplicates; most are small local-script mods or compatibility layers with manual reductions.

## Keep

- `modAllGearEnchantingBasedOnSockets`
- `mod0BiA_ASL_Compatibility`
- `modAdditionalStashLocations`
- `mod0MirrorReward_BiA3`
- `modEvents`
- `dlcEvents`
- `modRealCheatingYenAndTriss`
- `modAlcoholFactory`
- `dlcalcoholfactory`
- `modQuestItemEquipment`

## Key Compatibility Decisions

### Quest Item Equipment

Keep guarded mode.

The broad shipped `game\...` scripts remain disabled because they would replace W3EE/AMM/Friendly Meditation/Extra Skill Slots paths. The local script remains active, and W3EE has the required `MutagensSyngergyBonusUpdateEX()` bridge method.

Skip the Alchemy Primer patch because Alchemy Primer is not installed.

### Alcohol Factory

Keep the main mod/DLC.

Skip the Alchemy Primer patch because Alchemy Primer is not installed and the patch replaces recipe-extension logic.

### All Gear Enchanting

Keep the manual merge.

The mod's full `enchantingMenu.ws` is disabled. Its socket-based applicability logic was manually merged into the active Better Icons W3EE enchanting menu while preserving W3EE enchantment progression rules.

Skip the Friendly Stash patch because Friendly Stash is not installed.

### Additional Stash Locations

Keep both ASL and the BiA compatibility patch.

The BiA patch wins only the overlapping hub files. Base ASL still contributes Skellige and level stash files, while the needed spawner call was manually grafted into the active merged player script. This is a paired install, not duplicate clutter.

### Events

Keep Events while the feature is wanted.

The duplicate standalone descriptor is disabled because BiA already supplies `EVENTS_MenuDescriptor`. Events still contributes menu/fact/journal behavior and its DLC package.

### Master Mirror Reward

Keep `mod0MirrorReward_BiA3`.

It is a BiA-specific bundle-only reward patch with no scripts.

### Real Cheating Yen And Triss

Keep installed, with settings managed through its menu XML. This is roleplay/quest-state behavior, not redundant with W3EE or BiA.

## Status

- No game files changed in this triage pass.
- No Script Merger action run.
- No launch test run in this pass.
