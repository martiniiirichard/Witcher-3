# Appearance Backlog Triage - 662, 780, 3287, 5576, 11091

Date: 2026-06-27

Scope:
- Wild Nudity Project 662
- AMM 780 / AMM W3EE Redux support
- MCME / Corvo Bianco optionals 3287
- Dilated Pupils 5576
- Even More Companions / romance optionals 11091

## Result

No new install action is recommended from this cluster right now. The active stack already contains the useful selected pieces, and several remaining downloads are either optional alternates, language files, or already represented by a later merged install.

## Active / Keep

| Mod | Current State | Reason |
| --- | --- | --- |
| `modNakedCollection` | Active, priority 110, content-only. | Installed all-in-one Wild Nudity collection. Individual 662 character archives were intentionally not layered on top to avoid duplicate body/appearance bundle collisions. |
| Naked Geralt 2183 selected folders | Active selected FOMOD defaults. | Installed from the all-in-one package as chosen variant folders. Later redundancy pass archived only the shadowed underwear vanilla fix, not the active body/texture choices. |
| `modAMM` + `mod00AMMW3EEReduxNG139b` | Active, priorities 17 and 15. | Base AMM plus W3EE Redux compatibility patch. Prior fixes grafted missing W3EE Redux methods and removed the invalid mutagen reference. Keep as a pair while AMM is desired. |
| `modSpawnCompanions` | Active, priority 53, manually grafted. | Core MCME behavior is installed, but broad `r4Player.ws` and `npc.ws` forks were disabled and grafted into the current winners in `mod0000_MergedFiles`. Do not reinstall raw over the working merge. |
| Corvo Bianco selected variants | Active inside `modSpawnCompanions`. | User-selected relocation and sleeping arrangement variants were copied into MCME manager scripts. The archive choices are mutually exclusive, not cumulative. |
| `modCerys_5app02_S` | Active, priority 109. | Selected Cerys appearance is installed. |
| `modDilatedPupils` | Active, priority 124, content-only. | Standard pupils variant is installed. Black Irises is an alternate variant, not an add-on. |
| `modCorvoBiancoGuestsExtended` | Active, priority 155. | Separate Corvo Bianco guest content remains installed. |

## Skip / Already Covered

| Download | Decision |
| --- | --- |
| Individual `modNaked*` archives from 662 | Skip. `modNakedCollection` was installed as the all-in-one owner. Extra single-character archives would mostly create appearance overlap. |
| `modDilatedPupilsBlackIrises` | Skip unless the visual preference changes. It is an alternate to the installed standard `modDilatedPupils`, not a compatibility patch. |
| `AMM Polish Translation` 10958 | Skip for the English setup. |
| Raw AMM 780 add-ons already consumed by the AMM install | No action. `AMM 4.04 Scripts and Horse Mod`, `AMM 4.0 Menu Inputs and DLCs`, and the W3EE Redux patch are already represented in the active AMM install. |
| MCME 3287 base and optionals | No raw reinstall. Core MCME, enable collisions, Extended Cerys Romance, Corvo relocation, and sleeping arrangement were already handled in earlier passes. |
| 11091 companion/romance downloads | Mostly covered by the 8841 replacement `modSpawnCompanions`, selected Corvo variants, and installed companion appearance batch. Treat any future 11091 item as a variant choice, not an automatic add-on. |

## Guardrail

Do not copy raw `modSpawnCompanions`, AMM scripts, or MCME optional scripts over the current install without reapplying the W3EE merge pattern. These mods are working because broad player/NPC forks were disabled and only targeted behavior was grafted.
